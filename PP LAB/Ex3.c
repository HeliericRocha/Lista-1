#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x, y, z, w;
    printf("Digite 3 valores inteiros:");
    scanf("%d%d%d", &x, &y, &z);
    w = x + y + z;
    printf("%d",w);

    return 0;
}
